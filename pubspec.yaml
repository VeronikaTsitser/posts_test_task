name: posts_tets_task
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ^0.7.3
  flutter_bloc: ^8.1.6
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  dio: ^5.5.0+1
  cupertino_icons: ^1.0.8
  octo_image: ^2.0.0
  meta: ^1.12.0
  shimmer: ^3.0.0
  cached_network_image: ^3.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
